# volt-gui

> **My AMD Adrenaline / NVIDIA Settings Linux Alternative.**

volt-gui is being rebuilt. The environment variable era is over: settings
are now applied by **volt**, a Vulkan implicit layer that works on every
Vulkan 1.0 driver, and volt-gui is its graphical front end. Full
documentation lands with the rebuild.
